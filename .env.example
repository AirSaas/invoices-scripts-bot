# REQUIRED
OPENAI_API_KEY='api-key-123'

# CDP CONNECTION (connect to running Chrome with --remote-debugging-port)
CDP_PORT=9222

# EMAIL TO SEND INVOICES
GMAIL_COMPOSE_URL='https://mail.google.com/mail/u/0/#compose'
RECIPIENT_EMAIL='user@airsaas.io'

# EMAILS (used to select the right Google account if multiple are logged in)
FULLENRICH_EMAIL='user@airsaas.io'
BETTERCONTACT_EMAIL='user@airsaas.io'

# OPTIONAL - Only needed if you don't connect manually via CDP before running
# SEJDA_EMAIL='user@airsaas.io'
# SEJDA_PSW='psw123$'
# BETTERCONTACT_PSW='psw123$'
# FULLENRICH_PSW='psw123$'
# RECIPIENT_PSW='psw123$'
# GMAIL_PSW='psw123$'
